import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function AccountsPlaceholderPage() {
  return (
    <PlaceholderPage
      description="This route will become the social-account management workspace. It is live now so navigation, route protection, and shell responsiveness can be verified."
      eyebrow="Accounts"
      title="Connections workspace is staged."
    />
  );
}
