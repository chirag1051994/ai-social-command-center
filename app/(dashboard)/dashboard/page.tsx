import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function DashboardPlaceholderPage() {
  return (
    <PlaceholderPage
      description="The live metrics view is the next major build target. The shell is active now, so navigation, session gating, and inbox polling are in place for the full dashboard rollout."
      eyebrow="Dashboard Overview"
      title="Agency command center is wired."
    />
  );
}
