import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function AnalyticsPlaceholderPage() {
  return (
    <PlaceholderPage
      description="Analytics APIs are already wired. The chart-rich workspace and performance tables are the next frontend phase once the shell is locked in."
      eyebrow="Analytics"
      title="Performance reporting is queued."
    />
  );
}
