import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function ComposePlaceholderPage() {
  return (
    <PlaceholderPage
      description="This route is reserved for the AI post composer. The dashboard shell already routes here, and the real scheduling workflow lands in the feature-pages phase."
      eyebrow="Compose"
      title="Post composer arrives next."
    />
  );
}
