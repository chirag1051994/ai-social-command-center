import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function CalendarPlaceholderPage() {
  return (
    <PlaceholderPage
      description="The calendar route is scaffolded so navigation and mobile layout can be validated before the real scheduled-post grid and list views are added."
      eyebrow="Calendar"
      title="Scheduling view is staged."
    />
  );
}
