import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function InboxPlaceholderPage() {
  return (
    <PlaceholderPage
      description="Unread polling is active in the navigation now. The two-panel inbox experience will connect to the same API in the upcoming feature-page phase."
      eyebrow="Inbox"
      title="Unified conversations are next."
    />
  );
}
